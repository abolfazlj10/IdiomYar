# Archived source data

The files in `legacy-source` are retained for reference only. They were previously stored under `src/data/other`, but no application import or runtime reference uses them.

Keeping these source extracts outside `src` prevents build tools from scanning them as application inputs while preserving the original content and filenames.
